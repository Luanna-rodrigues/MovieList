import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyleSubTitle = styled(Typography)`
  && {
    font-size: 1em;
    display: flex;
    justify-content: center;
  }
`;

export default StyleSubTitle;
