import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledFirstGrid = styled(Grid)`
  padding-bottom: 30px;
  display: flex;
  justify-content: center;
`;

export default StyledFirstGrid;
