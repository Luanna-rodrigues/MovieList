import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledBottomGrid = styled(Grid)`
  padding-top: 20px;
  display: flex;
  justify-content: center;
`;

export default StyledBottomGrid;
