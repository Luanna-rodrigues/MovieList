import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridButton = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    padding-top: 100px;
  }
`;

export default StyledGridButton;