import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridTab = styled(Grid)`
  padding: 10px;
  display: flex;
  justify-content: center;
  border-radius: 10px;
`;

export default StyledGridTab;
